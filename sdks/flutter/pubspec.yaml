name: opencat
description: OpenCat SDK - Client SDK for in-app purchases and entitlement management.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_purchase: ^3.1.0
  flutter_secure_storage: ^9.0.0
  http: ^1.2.0
